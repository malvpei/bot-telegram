services:
  bot:
    build: .
    env_file:
      - .env
    environment:
      DATA_DIR: /app/data
    volumes:
      - bot_telegram_data:/app/data
    restart: unless-stopped

volumes:
  bot_telegram_data:
