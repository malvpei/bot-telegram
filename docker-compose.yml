services:
  bot:
    build: .
    env_file:
      - .env
    environment:
      DATA_DIR: /app/data
    volumes:
      - bot_telegram_data:/app/data
    ports:
      - "8000:8000"
    restart: unless-stopped

volumes:
  bot_telegram_data:
